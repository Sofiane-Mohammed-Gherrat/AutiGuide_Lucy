-To run the code :

 -Install Flask using: pip install "Flask>=3.0,<4.0"
 -Open a terminal and change the directory to the project folder using cd command
 -Run python app.py
 -open http://127.0.0.1:5000